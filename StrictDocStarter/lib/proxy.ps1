# StrictDocStarter - lib/proxy.ps1
# Reserved stub. Proxy auto-configuration is out of scope in v1.0 (FR-700).
# v2.x re-evaluates: see docs/setup-spec.md FR-700 and README "Proxy / Corporate Network".
#
# Runtime proxy detection (read-only, diagnostic) lives in lib/check.ps1 as
# Get-ProxyHint. This file is kept as a dot-source target for setup-strictdoc.ps1
# so the file structure (FR-806 + spec §3.3) remains stable across versions.

# (Intentionally empty: no functions exported in v1.0.)
